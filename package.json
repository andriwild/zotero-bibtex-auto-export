{
  "name": "zotero-bibtex-auto-export",
  "version": "0.4.0",
  "private": true,
  "description": "Zotero auto-export plugin (dev harness for unit tests only — not used at runtime).",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
